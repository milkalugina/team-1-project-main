<%= form_with(model: chat_list_item) do |form| %>
  <% if chat_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat_list_item.errors.count, "error") %> prohibited this chat_list_item from being saved:</h2>

      <ul>
        <% chat_list_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
